import xhrUtil from '../utils';

const login = () => (
  xhrUtil({
    
  })
);

const logout = () => (
  xhrUtil({

  })
);

const OAuthService = {
  login,
  logout,
};

export default OAuthService;
